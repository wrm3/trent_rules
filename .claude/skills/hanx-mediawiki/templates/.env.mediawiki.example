# MediaWiki Integration Configuration
#
# Copy this file to .env in your project root and configure with your MediaWiki details
#
# IMPORTANT: Add .env to your .gitignore to prevent committing credentials!

# MediaWiki API URL
# Format: https://your-wiki.com/api.php
# Examples:
#   - https://en.wikipedia.org/w/api.php (Wikipedia)
#   - https://wiki.example.com/api.php (Custom wiki)
#   - https://wiki.example.com/w/api.php (Alternative path)
MEDIAWIKI_API_URL=https://your-wiki.com/api.php

# Authentication Credentials
# For bot operations, use bot passwords (Special:BotPasswords)
# Format for bot: BotName@BotPassword
MEDIAWIKI_USERNAME=your_username
MEDIAWIKI_PASSWORD=your_password_or_bot_token

# Optional: Alternative credentials for different wikis
# Uncomment and configure if managing multiple wikis
# MEDIAWIKI_PROD_API_URL=https://prod-wiki.com/api.php
# MEDIAWIKI_PROD_USERNAME=prod_bot
# MEDIAWIKI_PROD_PASSWORD=prod_password

# MEDIAWIKI_DEV_API_URL=https://dev-wiki.com/api.php
# MEDIAWIKI_DEV_USERNAME=dev_bot
# MEDIAWIKI_DEV_PASSWORD=dev_password

# Optional: Default namespace for operations (0 = main namespace)
# MEDIAWIKI_DEFAULT_NAMESPACE=0

# Optional: Rate limiting (milliseconds between requests)
# MEDIAWIKI_RATE_LIMIT_MS=500

# Optional: Default edit summary prefix
# MEDIAWIKI_EDIT_SUMMARY_PREFIX="[Bot]"

# Optional: Maximum retries for failed requests
# MEDIAWIKI_MAX_RETRIES=3

# ============================================================================
# Setup Instructions:
# ============================================================================
#
# 1. Copy this file to .env in your project root:
#    cp templates/.env.mediawiki.example .env
#
# 2. Edit .env and add your MediaWiki credentials
#
# 3. Add .env to .gitignore:
#    echo ".env" >> .gitignore
#
# 4. (Recommended) Create a bot password:
#    a. Log in to your MediaWiki
#    b. Go to Special:BotPasswords
#    c. Create new bot password with needed permissions:
#       - High volume editing (for bulk operations)
#       - Edit existing pages
#       - Create, edit, and move pages
#       - Upload new files
#       - Upload, replace, and move files
#    d. Use the generated credentials in this file
#
# 5. Test connection:
#    python -c "from scripts.mediawiki_api import get_mediawiki_client; \
#               client = get_mediawiki_client(); \
#               client.login(); \
#               print('✓ Connected successfully')"
#
# ============================================================================
# Security Notes:
# ============================================================================
#
# - NEVER commit .env files to version control
# - Use bot passwords instead of your main account password
# - Grant minimum required permissions to bot accounts
# - Rotate passwords regularly
# - Use HTTPS URLs only
# - Consider using environment-specific credentials
# - Monitor bot activity through MediaWiki logs
#
# ============================================================================
# Troubleshooting:
# ============================================================================
#
# Q: Getting "Login failed" error?
# A: - Verify username and password are correct
#    - For bots, use format: BotName@BotPassword
#    - Check bot has necessary permissions
#    - Verify API URL is correct (must end with /api.php)
#
# Q: Getting "API URL not found" error?
# A: - Check MEDIAWIKI_API_URL is set correctly
#    - Verify URL is accessible
#    - Try accessing URL in browser (should show API help)
#
# Q: Getting permission errors?
# A: - Check bot has required permissions in Special:ListGroupRights
#    - Verify bot password has necessary grants
#    - Some operations require additional user rights
#
# ============================================================================
