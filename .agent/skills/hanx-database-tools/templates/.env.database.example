# Database Configuration Template
# Copy this file to .env in your project root and fill in your database credentials

# ============================================================================
# MySQL Configuration
# ============================================================================

# MySQL Source Database (Read operations)
src_mysql_host=localhost
src_mysql_port=3306
src_mysql_db=source_database
src_mysql_user=source_user
src_mysql_pw=source_password

# MySQL Target Database (Write operations)
tgt_mysql_host=localhost
tgt_mysql_port=3306
tgt_mysql_db=target_database
tgt_mysql_user=target_user
tgt_mysql_pw=target_password

# ============================================================================
# Oracle Configuration
# ============================================================================

# Oracle Source Database (Read operations)
src_db_host=oracle-server.company.com
src_db_port=1521
src_db_service=ORCL              # Service name (not SID)
src_db_user=source_user
src_db_pw=source_password
src_db_schema=SOURCE_SCHEMA      # Optional: Default schema to use

# Oracle Target Database (Write operations)
tgt_db_host=oracle-server.company.com
tgt_db_port=1521
tgt_db_service=ORCL_TARGET
tgt_db_user=target_user
tgt_db_pw=target_password
tgt_db_schema=TARGET_SCHEMA

# ============================================================================
# Security Notes
# ============================================================================
#
# 1. NEVER commit the actual .env file to version control
#    - Add .env to your .gitignore file
#
# 2. Use strong passwords for database accounts
#    - Minimum 12 characters
#    - Mix of uppercase, lowercase, numbers, symbols
#
# 3. Use read-only accounts for source databases when possible
#    - Grant only SELECT permission
#    - Prevents accidental modifications
#
# 4. Rotate credentials regularly
#    - Update passwords every 90 days
#    - Use different passwords for each environment
#
# 5. Environment-specific configuration
#    - Use different .env files for development, staging, production
#    - Store production credentials in secure vault (e.g., AWS Secrets Manager)
#
# ============================================================================
# Connection Testing
# ============================================================================
#
# Test MySQL connection:
#   python scripts/mysql_db.py
#
# Test Oracle connection:
#   python scripts/oracle_db.py
#
# ============================================================================
# Additional MySQL Parameters (Optional)
# ============================================================================
#
# MySQL connection timeout (seconds)
# MYSQL_CONNECT_TIMEOUT=30
#
# MySQL autocommit mode (true/false)
# MYSQL_AUTOCOMMIT=true
#
# MySQL charset
# MYSQL_CHARSET=utf8mb4
#
# ============================================================================
# Additional Oracle Parameters (Optional)
# ============================================================================
#
# Oracle connection pool size
# ORACLE_POOL_SIZE=10
#
# Oracle connection pool max overflow
# ORACLE_POOL_MAX_OVERFLOW=5
#
# Oracle thick mode (requires Oracle Instant Client)
# ORACLE_THICK_MODE=false
